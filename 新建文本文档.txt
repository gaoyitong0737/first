git init 

git remote add name repository_URL

gti branch -M name

git add filename
	. -> all files
	
	git add .  


git commit -m "commit content" 
	
	git commit -a -m "commit content" = git add .  ;  git commit -m "commit content"
git push -u 仓库名 分支名

git status 查看状态
